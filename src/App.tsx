/**
 * Scaffold placeholder. Replaced by the assembled Match-3 run after the kit's 2D
 * modules (board/render2d/fx2d/campaign/theme/tuning) are vendored in.
 */
export function App() {
  return (
    <div
      style={{
        display: "grid",
        placeItems: "center",
        height: "100%",
        color: "#cdd3e6",
        fontFamily: "system-ui, sans-serif",
        background: "linear-gradient(#141a2e, #0b0d16)",
      }}
    >
      <p>Cascade — scaffold ready.</p>
    </div>
  );
}
